import React, { Fragment } from 'react'

export const ItemDetail = () => {
    return (
        <Fragment>
            {/* id={product.id}
            name={product.name}
            img={product.img}
            price={product.price}
            type={product.type} */}
        </Fragment>
    )
}
